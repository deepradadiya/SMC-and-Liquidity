# Production environment — used by Vercel automatically
# Binance WebSocket connects directly from browser — no proxy needed for prices/charts
# Only set REACT_APP_PROXY_URL if you deploy the proxy server (for signal engine)
REACT_APP_PROXY_URL=
